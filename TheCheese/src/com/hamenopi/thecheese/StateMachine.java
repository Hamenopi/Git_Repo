package com.hamenopi.thecheese;

import java.util.HashMap;
import java.util.Map;

import com.hamenopi.thecheese.graphics.Screen;

class StateMachine
{
  /*  Map<String, IState> mStates = new HashMap<String, IState>();
    IState mCurrentState = Menu;
 
    public void update()
    {
        mCurrentState.update();
    }
 
    public void Render(Screen screen)
    {
        mCurrentState.render(screen);
    }
 
    public void change(String stateName)
    {
        mCurrentState.onExit();
        //mCurrentState = mStates[stateName];
        mCurrentState.onEnter();
    }
 
    public void add(String name, IState state)
    {
        //mStates[name] = state;
    }*/
}